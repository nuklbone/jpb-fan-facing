import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join the James Pitts Band Inner Circle | Fan Club & Presales",
  description:
    "Join the James Pitts Band Inner Circle for presale alerts, exclusive live recordings, merch perks, and behind-the-scenes drops.",
};

export default function JoinLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

